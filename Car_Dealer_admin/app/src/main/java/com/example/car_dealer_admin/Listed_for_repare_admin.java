package com.example.car_dealer_admin;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;

public class Listed_for_repare_admin extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_listed_for_repare_admin);

      //  getSupportFragmentManager().beginTransaction().replace(R.id.repare_layout,new refregment_repare()).commit();


    }
}